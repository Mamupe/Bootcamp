//Iteración #1: Interacción con el DOM
//1.1 Usa querySelector para mostrar por consola el botón con la clase .showme
const queryButton = document.querySelector(".showme");
console.log(queryButton);
//1.2 Usa querySelector para mostrar por consola el h1 con el id #pillado
const queryH1 = document.querySelector("#pillado");
console.log(queryH1);
//1.3 Usa querySelector para mostrar por consola todos los p
const parrafos = document.querySelectorAll("p");
console.log(parrafos);
//1.4 Usa querySelector para mostrar por consola todos los elementos con la clase.pokemon
const pokes = document.querySelectorAll(".pokemon");
console.log(pokes);
//1.5 Usa querySelector para mostrar por consola todos los elementos con el atributo data-function="testMe".
const data = document.querySelectorAll('span');
console.log(data);
//1.6 Usa querySelector para mostrar por consola el 3 personaje con el atributo data-function="testMe".
const span3 = document.querySelector("span:Rick");
console.log(span3);




